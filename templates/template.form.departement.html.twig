{{form_start(form)}}
<div class="my-custom-class-for-errors">
    {{ form_errors(form) }}
</div>
<div class="row">
    <div class="col-md-4"> {{form_row(form.nom)}}</div>
    <div class="col-md-4"> {{form_row(form.nombreEmployer)}}</div>
</div>

<button type="submit" class="btn btn-success btn-sm fa fa-user">  {{ button|default('Enregistrer')}} </button>
{{form_end(form)}}